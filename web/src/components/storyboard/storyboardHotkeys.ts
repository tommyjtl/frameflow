import type { Options } from 'react-hotkeys-hook'

/** Shared defaults for storyboard keyboard shortcuts. */
export const STORYBOARD_HOTKEY_OPTIONS: Options = {
  enableOnFormTags: false,
  enableOnContentEditable: false,
}
